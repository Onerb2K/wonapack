/*
● Receber do usuário um vetor de inteiros (0 ou 1) de 8 posições;
● Imprimir o valor decimal do número em binário expresso pelo vetor;
● Receber do usuário um número inteiro referente ao deslocamento à esquerda
da instrução SLL;
● Imprimir o v
etor resultante após o SLL;
● Imprimir o valor decimal do número em binário expresso pelo vetor resultante.
*/
#include <stdio.h>
#define tam 8
#include <math.h>
int main(void)
{
    int vetor[tam];
    int j = 0,dec = 0;
    int sll;
    int i;
    int m =1 ;
    for( i = 0 ; i < tam ; i++)  // Para preencher o vetor precisamos de um laço
    {
        printf("Entre com os elementos do vetor de tam 8 , APENAS 0s e 1s\n" );
        scanf("%d",&vetor[i]); // cada posição do vetor recebe um valor
    }
    for( i = 0 ; i < tam ; i++){ // Para imprimir um vetor tambem precisamos de um laço
		printf("elemento %d : %d \n",j, vetor[i] ); // imprimindo os elementos 1 por 1
		j++;
    }
    // for para transformar o número de bin para dec
     for(i=tam-1; i>-1; i--){
        if( vetor[i] == 1 ){
            dec = dec + vetor[i]*m;
        }
        m = m*2;
    }
    printf("O numero convertido para decimal eh: %d \n",dec );
// if ( i == 1 ) vetor[i] = 0; Ideia de resolucao
    printf("Entre com o valor para SLL \n");
    scanf("%d",&sll);
    j = 0; // só pra resetar o print mesmo

       /* for(i = tam ; sll <= i; i--){
       ?for(i = tam ; sll => i; i--)
       for(i = tam ; i => tam-sll; i--){ algo assim sla
                // i = 3 ; 3 >= 1; 2
                // i = 2 ; 2 >= 1 ; 1
                // i = 1 ; 1 = 1 ; 0
                // i = 0 ; fim
                // so temos que transformar os sll ultimos em 0
                vetor[i]  = 0; // elemnto 3 = 0
                // elemento 2 = 0;
                // elemento 1 = 0;
        }*/
	
	/* ANOTAÇÃO DA AULA 
	SLL 2 / I = I+2
	for(i = 0; i >
	*/
        for(i = tam ; i < tam ; i++){
		printf("elemento %d depois do SLL: %d \n",j, vetor[i] ); // imprimindo os elementos 1 por 1
		j++;
        }
      /*for(i = 0; vetor[i] > 0; i++)
    {
        dec = dec + pow(2, i) * (vetor[i] % 10);
        vetor[i] = vetor[i] / 10;
    }
    printf("O valor decimal equivalente %d\n", dec);

   for (i = tam-1; i >= 0; i--) {
		//printf("%c|", numero[i]);
		if (vetor[i] == '1') {
			dec = dec + pow(2,tam-1-i);
		}
	}
	printf("Decimal: %i", dec);
	*/
	}
